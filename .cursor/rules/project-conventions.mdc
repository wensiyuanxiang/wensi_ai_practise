---
description: Python AI Agent 项目规约 - 统一日志、统一使用 shared 公共工具与 LLM
alwaysApply: true
---

# 项目规约（本仓库）

本仓库为 **Python AI Agent 实践项目**，编写或修改代码时请遵守以下规约。

## 统一日志与打印

- **禁止**在业务逻辑中用裸 `print()` 输出进度、结果或调试信息；一律使用 `logging`。
- 脚本入口处先调用 **`shared.logging_config.setup_logging()`** 完成统一日志配置（格式、级别）。
- 模块内使用 `logger = logging.getLogger(__name__)`，通过 `logger.info` / `logger.debug` / `logger.warning` / `logger.error` 输出。
- 日志中**不得**包含 API Key、Token、密码、完整用户输入等敏感信息。

## 使用公共工具（shared）

- **模型调用与 LLM 配置**：必须通过 `shared` 提供的方式，不在实验脚本中硬编码 API 地址或 Key。
  - LangChain：使用 `shared.llm.aliyun_llm.get_aliyun_llm()` / `get_openai_llm()`。
  - CrewAI 等：使用 `shared.llm.aliyun_llm.get_aliyun_llm_env()` 得到配置后传入框架 LLM（如 `crewai.LLM(**get_aliyun_llm_env())`）。
- **自定义工具**（如百度搜索）：从 `shared.tools` 导入（如 `from shared.tools import baidu_search_tool`），不复制实现。
- 环境变量由项目根目录 `.env` 管理；脚本中通过 `load_dotenv(project_root / ".env")` 加载。

## 路径与结构

- 实验脚本需将**项目根目录**加入 `sys.path`，再 `import shared.*`。
- 详细说明见 `docs/architecture/20260313-project-conventions.md`。
